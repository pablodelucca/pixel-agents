{
  "name": "pixel-agents",
  "description": "Pixel art office where your AI agents come to life as animated characters",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "cd webview-ui && npm run dev",
    "build": "cd webview-ui && npm run build",
    "preview": "cd webview-ui && npm run preview",
    "install:all": "npm install && cd webview-ui && npm install"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
