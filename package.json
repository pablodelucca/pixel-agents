{
  "name": "pixel-agents-remote",
  "description": "Remote monitoring for Claude Code agents - pixel art office visualization",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "npm run dev:server & npm run dev:webview",
    "dev:server": "cd remote-server && npx tsx server.ts",
    "dev:webview": "cd webview-ui && npm run dev",
    "build": "cd webview-ui && npm run build",
    "start": "npm run dev:server & npm run dev:webview"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}