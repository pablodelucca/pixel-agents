services:
  - type: web
    name: pixel-office
    runtime: docker
    region: frankfurt
    plan: standard
    envVars:
      - key: PORT
        value: "3000"
      - key: ANTHROPIC_API_KEY
        sync: false
