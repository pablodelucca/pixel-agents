

## WORKING MEMORY
[2026-03-18T01:32:37.969Z] 2026-03-18: Functional validation baseline completed. npm ci succeeded in root and webview-ui; npm run check-types, npm run lint, webview-ui npm test, and npm run build all pass. Real local Codex logs show tool names exec_command/write_stdin/update_plan/spawn_agent/wait; current parser maps these to generic 'Using ...'. Synthetic parser probes confirm shell_command/apply_patch/read_file map well, but web_search_call extracts as Grep due STATUS_TO_TOOL prefix order, request_user_input extracts as Waiting, and spawn_agent remains generic. Waiting timer and permission timer behavior verified via ad hoc scripts against real session lines.

[2026-03-18T07:06:16.619Z] 2026-03-18: Fixed three blockers for visual Codex verification: launch now uses plain `codex` instead of unsupported `--session-id`; file watcher leaves relevant new JSONLs unclaimed until they can be adopted/reassigned, preventing focus-timing misses; `.vscode/launch.json` now opens the current workspace in the Extension Host so `vscode.workspace.workspaceFolders` exists. Added a user-facing warning when launching without a workspace. check-types, lint, and build all pass.
[2026-03-18T08:23:33.178Z] 2026-03-18 structure validation: minimum Codex visualization works in another repo via hover/status, but architecture is still transitional. Evidence: src/agentAdapter.ts unused; package.json pixel-agents.agentType not wired; src/agentManager.ts/getProjectDirPath hardcodes ~/.codex/sessions and launch command; src/fileWatcher.ts directly scans/adopts/parses via processTranscriptLine; src/transcriptParser.ts mixes Codex and Claude logic; UI still sends openClaude messages. Next plan: adapter factory from config, per-backend adapters, neutral openAgent message, contract tests, and Codex tool-name animation mapping fixes.